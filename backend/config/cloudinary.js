// Cloudinary removed — all files are stored in MongoDB GridFS.
// File serving: GET /api/files/:id  (public, no auth required)
// Avatar upload: PATCH /api/upload/avatar  (authenticated)
export const UPLOAD_PRESETS = {};
export default {};
